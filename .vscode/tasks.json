{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compile", // task name, it will be used in launch.json.
            "type": "npm",
            "script": "compile" // compile task has been defined in scripts of package.json.
        }
    ]
}